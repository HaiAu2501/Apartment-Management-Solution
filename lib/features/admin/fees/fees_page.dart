import 'package:flutter/material.dart';

class FeesPage extends StatelessWidget {
  const FeesPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Quản Lý Phí Dịch Vụ'),
      ),
      body: const Center(
        child: Text('Chức năng quản lý phí dịch vụ sẽ được phát triển sau.'),
      ),
    );
  }
}
